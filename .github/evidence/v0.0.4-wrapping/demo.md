# Wrapping in action

**Styled text stays intact** as this paragraph folds across narrow terminal lines. Accents such as café and éclair retain their characters, and words keep the same line breaks as before.

A second paragraph demonstrates that hard paragraph boundaries remain separate from automatic wrapping. **The output is unchanged; long lines render faster.**
