# Normalize line endings to LF in the repo for everything text.
# Keeps byte-sensitive golden fixtures and cross-platform CI consistent.
* text=auto eol=lf

# Binary artifacts retain exact capture bytes.
*.png binary
*.ico binary
*.sixel binary
